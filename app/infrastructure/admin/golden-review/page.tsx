// Golden-set review lives inside the getvya.ai workspace (Platform group), next to AI accuracy —
// it's the answer key that dashboard's numbers grade against. Reuses the /admin component.
export { default } from "@/app/admin/golden-review/page";
